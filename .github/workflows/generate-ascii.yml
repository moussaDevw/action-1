name: Generate ASCII Art

on: push

jobs:
 ascii_job:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout repos
          uses: actions/checkout@v7

        - name: List files
          run: ls -ltra
          
        - name: Executing shell script
          run: ./ascii-script.sh